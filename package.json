{
  "name": "frontend-dev",
  "version": "1.0.0",
  "description": "frontend-dev",
  "main": "app.js",
  "scripts": {
    "all": "npm-run-all -p sass tsc browser-sync",
    "tsc": "tsc",
    "sass": "sass ./scss/:./css/ --watch",
    "browser-sync": "browser-sync start --config bs-config.js"
  },
  "author": "steven",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^4.1.5"
  }
}
